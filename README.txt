How to compile my code:

Enter the following command to compile my code with an executible titled smallsh
    gcc --std=gnu99 -o smallsh smallsh.c


This is a custom shell program built in C. It implements background processes, built-in commands, signal handling, and input/output redirection. 


